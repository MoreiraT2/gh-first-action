name: First workflow
on: workflow_dispatch
jobs:
  first-job:
      runs-on: ubuntu-latest
      steps:
        - name: Print greeting
          run: echo "Good morning World"
        - name: Print GoodBye
          run: echo "Done, Bye"
